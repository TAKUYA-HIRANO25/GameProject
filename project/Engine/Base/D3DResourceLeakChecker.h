#pragma once
/// <summary>
/// D3D リソースのリーク検査ユーティリティクラス（ヘルパー）.
/// 
/// 目的:
/// - デバッグ終了時に Direct3D のリソースが全て解放されているかを検査するための RAII 風ヘルパー。
/// - デストラクタで Direct3D のデバッグレイヤーやレポート関数を呼び出す想定（実装ファイル側で行う）。
/// 
/// 使用方法:
/// - アプリケーションのエントリポイントでローカルオブジェクトとして生成しておくだけで、
///   スコープ終了（アプリ終了）時に検査が行われる。
/// 
/// 備考:
/// - 実際の検査処理は実装（.cpp）に依存します。リリースビルドでは何もしない実装にすることが一般的です。
/// </summary>

class D3DResourceLeakChecker {
public:
	/// <summary>
	/// デストラクタ
	/// </summary>
	~D3DResourceLeakChecker();
};